---
import BaseLayout from '../layouts/BaseLayout.astro';
const { title, subtitle, description,
  date, placement, hidden, layout } = Astro.props.frontmatter;
---
<BaseLayout pageTitle={title}>
  <slot/>
</BaseLayout>
